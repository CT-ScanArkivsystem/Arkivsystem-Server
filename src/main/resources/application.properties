# used data-source
# dataSource.serverName=${ENV=DB_HOST}
# dataSource.port=5432
# dataSource.databaseName=${ENV=DB_BASE}
#
# dataSource.user=${ENV=DB_USER}
# dataSource.password=${ENV=DB_PASSWD}

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.hibernate.show-sql=true
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=${ENV=DB_USER}
spring.datasource.password=${ENV=DB_PASSWD}

spring.datasource.initialization-mode=always
spring.datasource.initialize=true
spring.datasource.schema=classpath:/schema.sql
spring.datasource.continue-on-error=true